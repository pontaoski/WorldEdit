namespace WorldEditSchematicTranslator;

internal partial class Program
{
    private const byte OLD_ILLUMINANT_PAINT_ID = 31;
    private const ushort MAX_TILE_ID = 692;
    private const int BUFFER_SIZE = (1024 * 1024);
}